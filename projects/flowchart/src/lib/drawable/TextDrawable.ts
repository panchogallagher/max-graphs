import { IDrawable } from './IDrawable';
import { Layer } from 'konva/types/Layer';
import { Node } from '../object/node';

export class TextDrawable implements IDrawable {

    draw(layer:Layer, node:Node) {

    }
}